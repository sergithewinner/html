const name = ' brad ';

function name() {
  console.log('test');
}

console.log('hello');

console.error(object);

const asd = (asd) => {
  console.log(object);
};

const name = (params) => {
  // nfn
};

array.forEach((currentItem) => {
  // fre
});
